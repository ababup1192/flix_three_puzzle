{
  "//": "three_puzzle.rules.json の形の宣言（Studio の sections 方言）。数値と先手だけ Doc に置き、勝ち負けの判定はコード（src/Board.flix）。",
  "version": 1,
  "sections": {
    "firstTurn": {
      "kind": "field",
      "type": {
        "enum": [
          "o",
          "x"
        ]
      },
      "label": "先手（どちらのマーカーから始めるか）",
      "order": 1,
      "default": "o"
    },
    "moveSeconds": {
      "kind": "field",
      "type": "float",
      "label": "主人公が隣のマスへ移る時間（秒）",
      "order": 2,
      "min": 0.02,
      "max": 1.0,
      "default": 0.14
    },
    "dropHeight": {
      "kind": "field",
      "type": "float",
      "label": "置いたマーカーが落ちてくる高さ（px。0 で弾まない）",
      "order": 3,
      "min": 0,
      "max": 60,
      "default": 14
    }
  }
}